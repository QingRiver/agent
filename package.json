{
  "name": "agent",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.27.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "keywords": [
    "agent",
    "ai",
    "chatbot"
  ],
  "engines": {
    "node": ">=24.0.0 <25.0.0"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter server --filter client dev",
    "cli": "pnpm --filter @agent/cli start",
    "tc": "pnpm -r run tc",
    "tsc-go": "tsx scripts/tsc-go.ts",
    "lint": "pnpm exec eslint apps packages",
    "format": "pnpm exec eslint apps packages --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "qlib:package": "tsx scripts/qlib-package-source.ts package",
    "qlib:unpack": "tsx scripts/qlib-package-source.ts unpack",
    "qlib:init": "tsx scripts/qlib-daily-update.ts init",
    "qlib:update": "tsx scripts/qlib-daily-update.ts update"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:",
    "@eslint-react/eslint-plugin": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "eslint-plugin-format": "catalog:",
    "ts-pattern": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "typescript-go": "catalog:",
    "vitest": "catalog:"
  }
}
