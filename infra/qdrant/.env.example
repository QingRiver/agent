# Qdrant 向量库（infra/qdrant/）
# 共享配置从仓库根 .env 读取（docker-compose env_file: ../../.env）

# REST API 端口（默认 6333）
QDRANT_API_PORT=6333

# gRPC 端口（默认 6334）
QDRANT_GRPC_PORT=6334

# 集合名前缀（packages/kb 拼接 kbId 时使用，可选）
QDRANT_COLLECTION_PREFIX=kb_
